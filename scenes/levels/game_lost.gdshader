shader_type canvas_item;

uniform float grayness = 1.0;
uniform float progress = 0.0;

void fragment() {
	//vec4 bg = texture(SCREEN_TEXTURE, SCREEN_UV);
	//COLOR.rgba = vec4(0.299 * COLOR.r + 0.587 * COLOR.g + 0.114 * COLOR.b, COLOR.a);
}
